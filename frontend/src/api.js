// frontend/src/api.js

const BASE_URL = 'http://localhost:5000/api'; // Replace with your actual backend URL

export default {
    todos: `${BASE_URL}/todos`, // Update this with your backend API endpoints
};
